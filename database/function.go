package database

/*
import (
	"context"

	"github.com/jackc/pgx/v5"
)

func createUser(DB *pgx.Conn, player Player) {
	err := DB.QueryRow(context.Background(),
		`insert into player
		(username,level,exp,po,job_id,rank,city)`
	)
}*/
